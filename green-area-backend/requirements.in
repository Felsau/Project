# Direct dependencies (human-edited source of truth).
# Compile the fully-pinned, hashed lockfile with:
#
#   uv pip compile requirements.in --generate-hashes -o requirements.txt
#
# (or: pip-compile --generate-hashes requirements.in)
#
# requirements.txt is the generated lock — install from THAT, not this file.
fastapi==0.136.3
uvicorn[standard]==0.49.0
supabase==2.31.0
python-dotenv==1.2.2
earthengine-api==1.7.31
httpx==0.28.1
matplotlib==3.10.9
Pillow==12.2.0
requests==2.34.2
slowapi==0.1.9

# Dev/test only
pytest==9.0.3
