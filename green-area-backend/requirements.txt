# Pinned for reproducible, tamper-resistant deploys (matches the working venv).
# A full lockfile (pip-tools / uv) that also pins transitive deps is the next step.
fastapi==0.136.3
uvicorn[standard]==0.49.0
supabase==2.31.0
python-dotenv==1.2.2
earthengine-api==1.7.30
httpx==0.28.1
matplotlib==3.10.9
Pillow==12.2.0
requests==2.34.2
slowapi==0.1.9

# Dev/test only
pytest==9.0.3
